using UnityEngine;
using System.Collections;

public class RockBehaviour : MonoBehaviour
{
    // Use this for initialization
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.O))
        //{
        //    // todo 手の開閉状態をどう検出するか
        //    avgOpen = uhand.UHPR[1];// + uhand.UHPR[1] + uhand.UHPR[2] + uhand.UHPR[3];
        //    Debug.Log("set open pr2: " + avgOpen);
        //}
        //if (Input.GetKeyDown(KeyCode.C))
        //{
        //    avgClose = uhand.UHPR[1];// + uhand.UHPR[1] + uhand.UHPR[2] + uhand.UHPR[3];
        //    Debug.Log("set close pr2: " + avgClose);
        //}
        //if (avgClose + margin > uhand.UHPR[1] || Input.GetKeyDown(KeyCode.Alpha1))// + uhand.UHPR[1] + uhand.UHPR[2] + uhand.UHPR[3])
        //{
        //    // hand close.
        //    closeHandAction();
        //}
        //else if (avgOpen - margin < uhand.UHPR[1] || Input.GetKeyDown(KeyCode.Alpha2))
        //{
        //    // hand open.
        //    openHandAction();
        //}
    }
}
